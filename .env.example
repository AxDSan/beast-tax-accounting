# Supabase Configuration
GATSBY_SUPABASE_URL=your_supabase_project_url_here
GATSBY_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Alternative environment variable names (also supported)
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Note: For the admin panel to work fully, you need to:
# 1. Set up a Supabase project
# 2. Create a 'brand_config' table with columns: id (integer, primary key), config_data (jsonb), updated_at (timestamp)
# 3. Create a storage bucket called 'brand-assets' with public access
# 4. Configure the S3 credentials in the API file if using custom storage
